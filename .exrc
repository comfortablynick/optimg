let b:coc_disable_cursorhold_hover = 1

let b:project_root_dir = expand('$HOME/git/optimg')

let b:ft_cmds = {
        \ 'go': {
        \   'build': ' just build',
        \   'install': ' just install',
        \   'run': ' just run',
        \  }
        \ }
